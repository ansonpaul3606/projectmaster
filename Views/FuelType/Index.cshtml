
@{
    ViewBag.Title = "Index";
}


<div class="container-fluid">

    <div class="row">
        <div class="col-lg-12" id="fuel-sample-form">

        </div>
    </div>


</div>

@section scripts{

    <script>


        


        const $formContainer = $('#fuel-sample-form');
    $(document).ready(function () {

            initialloadFuelform();
        });

        function initialloadFuelform() {
              loadAddModelForm();
              @if (ViewBag.PagedAccessRights.UsrrlMsView){
                 <text>loadFuelList(this); </text>
               }
        }


        function loadAddModelForm() {
            debugger
             $formContainer.load("@Url.Action("FuelTypeView", "FuelType")", function () {
                let $thisForm = $(this).find('[perfect-class="form"]');           
                 $thisForm.attr('perfect-onFormSuccess', 'initialloadModelform');
            });
        }


        function loadFuelList(ele) {
            let $sidemenu = $sideMenuOptions.body;

            let $thisForm = $(ele).closest('.testSearchBox');
            let SearchName = $thisForm.find('input').val();
        }
    













    </script>


}
