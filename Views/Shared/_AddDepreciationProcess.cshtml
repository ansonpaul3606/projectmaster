
@model PerfectWebERP.Models.DepreciationProcessModel.DepreciationProcessView


<div class="card">

    <div class="card-header">
        <h4 class="card-title col-12">Depreciation Process</h4>

        <span title="To View List" onclick="fn_showListView();"><svg id="Layer_1" class="layer" stroke="currentColor" data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" height="20" width="20" viewBox="0 0 122.88 122.54"><title>To View List</title><path class="viewlst" d="M4.69,0H46.22a4.71,4.71,0,0,1,4.69,4.69V46a4.69,4.69,0,0,1-4.69,4.69H4.69a4.65,4.65,0,0,1-3.31-1.38l-.09-.09A4.67,4.67,0,0,1,0,46V4.69A4.71,4.71,0,0,1,4.69,0ZM89.44,61.94a26.56,26.56,0,0,1,10.18,2l.07,0a26.61,26.61,0,0,1,15.25,32.16,26.18,26.18,0,0,1-2.7,6.11l10.3,11.24a1.27,1.27,0,0,1-.07,1.8l-7.57,6.9a1.27,1.27,0,0,1-1.79-.07l-9.86-10.85a26.36,26.36,0,0,1-6.1,2.74,26.87,26.87,0,0,1-7.71,1.13,26.51,26.51,0,0,1-10.17-2l-.07,0A26.64,26.64,0,0,1,64.85,78.37l0-.07A26.6,26.6,0,0,1,89.44,61.94Zm15,11.59a21.38,21.38,0,0,0-6.89-4.61l-.06,0a21.22,21.22,0,0,0-23.07,4.64l-.07.07a21.25,21.25,0,0,0-4.54,6.83l0,.06a21.32,21.32,0,0,0-1.58,8.06,21.26,21.26,0,0,0,29.35,19.62,21.54,21.54,0,0,0,6.89-4.61l.07-.07a21.09,21.09,0,0,0,4.54-6.83l0-.06a21.35,21.35,0,0,0,0-16.17,21.34,21.34,0,0,0-4.62-6.9ZM4.69,63.2H46.22a4.71,4.71,0,0,1,4.69,4.7v41.34a4.68,4.68,0,0,1-4.69,4.69H4.69A4.69,4.69,0,0,1,0,109.24V67.9a4.71,4.71,0,0,1,4.69-4.7ZM68.78,0h41.53A4.71,4.71,0,0,1,115,4.69V46a4.71,4.71,0,0,1-4.69,4.69H68.78A4.71,4.71,0,0,1,64.09,46V4.69a4.69,4.69,0,0,1,1.37-3.31l.1-.09A4.67,4.67,0,0,1,68.78,0Z"></path></svg></span>
    </div>

    <div class="card-body">
        <form id="" class="form-valide" perfect-class="form" action="#" method="post" perfect-onFormSuccess="">

            <div class="">
                @Html.AntiForgeryToken()

                <div class="row">

                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">
                                <span perfect-class="formGroupLabel">Process Date</span>
                                <span class="text-danger">*</span>

                            </label>
                            <div class="col-sm-8">
                                <input type="date" class="form-control perfectValidate_date" name="ProcessDate" perfect-class="formGroupControl" perfect-ctype="date" perfect-css="date" readonly />
                            </div>
                        </div>
                    </div>

                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">
                                &nbsp;&nbsp;&nbsp;

                                <span perfect-class="formGroupLabel">From Date</span>
                                <span class="text-danger">*</span>
                            </label>
                            <div class="col-sm-8">
                                <input type="date" class="form-control perfectValidate_date" name="Fromdate" perfect-class="formGroupControl" perfect-ctype="date" perfect-css="date" max="2050-01-01" onchange="checkLimit(this)" />
                                <div class="invalid-feedback animated fadeInUp limit-check" style="display: block;"></div>

                                @*<div id="Todate-error" class="Custominvalid animated fadeInUp Todateerror" style="display: none;">To Date sholud be greater than  Or Equal to From Date</div>*@

                            </div>
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">
                                &nbsp;&nbsp;&nbsp;

                                <span perfect-class="formGroupLabel">To Date</span>
                                <span class="text-danger">*</span>
                            </label>
                            <div class="col-sm-8">
                                <input type="date" class="form-control perfectValidate_date" name="Todate" perfect-class="formGroupControl" perfect-ctype="date" perfect-css="date" max="2050-01-01" onchange="checkLimit(this)" />
                                <div class="invalid-feedback animated fadeInUp limit-check" style="display: block;"></div>

                                @*<div id="Todate-error" class="Custominvalid animated fadeInUp Todateerror" style="display: none;">To Date sholud be greater than  Or Equal to From Date</div>*@

                            </div>
                        </div>
                    </div>

                    @*<div class="col-sm-4"></div>
        <div class="col-sm-4"></div>*@
                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">

                                <span perfect-class="formGroupLabel">Mode </span>

                                <span class="text-danger">*</span>
                                <input type="hidden" name="ID_DepreciationProcess" class="" perfect-class="formGroupControl" perfect-ctype="hidden" perfect-css="input" />

                            </label>
                            <div class="col-sm-8">
                                <div class="input-group">
                                    <select class="form-control perfectValidate_string" name="Mode" id="Mode" perfect-class="formGroupControl" data-id="subtableperiodmode-error" perfect-ctype="select" perfect-css="select" onchange="fn_showTypeList(this)">
                                        <option value="">Please Select </option>
                                        @if (!(Model.ModeList is null))
                                        {
                                            foreach (var ModeList in Model.ModeList)
                                            {
                                                <option value="@ModeList.ID_Mode">@ModeList.ModeName</option>
                                            }
                                        }
                                    </select>

                                </div>
                                <div class="text-danger temptableError"></div>
                            </div>

                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">
                             
                                <span perfect-class="formGroupLabel">Category</span>
                                <span class="text-danger">*</span>
                            </label>

                            <div class="col-sm-8">
                                <select class="form-control  perfectValidate_string" name="FK_Category"   id="Categoryid" perfect-tempTable-DepreciationProcess="Category" perfect-class="formGroupControl" perfect-ctype="select" perfect-css="select" data-live-search="true" onchange="clearproduct(this)">

                                    <option value="">Please Select </option>

                                    @if (!(Model.CategoryList is null))
                                    {
                                        foreach (var categorylist in Model.CategoryList)
                                        {
                                            <option value="@categorylist.FK_Category">@categorylist.Category</option>
                                        }

                                    }

                                </select>
                                <div class="text-danger temptableError"></div>
                            </div>
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-group row" perfect-class="formGroup">
                            <label class="col-sm-4 col-form-label">
                                &nbsp;&nbsp;&nbsp;
                                <span perfect-class="formGroupLabel"> Product</span>
                                @*<span class="text-danger">*</span>*@

                            </label>
                            <div class="col-sm-8">
                                <div class="input-group">
                                    <input type="text" class="form-control " name="Product" perfect-tempTable-DepreciationProcess="Product" perfect-class="formGroupControl" perfect-ctype="input" perfect-css="input" disabled />
                                    <input type="hidden" name="FK_Product" perfect-tempTable-DepreciationProcess="FK_Product" class="" perfect-class="formGroupControl" perfect-ctype="hidden" perfect-css="input" />
                                    <div class="input-group-append">

                                        <button id="addproduct-jq-searchButton3" class="btn btn-primary fa fa-search" type="button" perfect-css="button" onclick="fn_showlists(this)"  Function="2" Criterea1="FK_Category" Criterea2="0" BindName="Product" BindVal="FK_Product"></button>

                                    </div>
                                </div>

                            </div>

                        </div>

                    </div>
                    @*<div class="col-sm-4">

            <button class="btn btn-primary1" type="button" data-toggle="tooltip" data-placement="top" data-html="true" title="Add New Depreciation Process" onclick="GetDepreciationProcessDetails(this)">Process</button>

        </div>*@

                    <div class="col-sm-4" >

                        <button class="btn btn-primary1" type="button" data-toggle="tooltip" data-placement="top" data-html="true" title="Add New Attendance Marking" onclick="fn_DepreciationProcess(this)">Process</button>

                    </div>


                </div>


            </div>

            @*<div class="row">
            <div class="col-sm-12" style="padding-right:25px;padding-left:25px;">
                <div class="form-group row" perfect-class="formGroup">
                    <div class="table-responsive" perfect-tempTable="DepreciationProcess" id="DepreciationProcessItemHead" perfect-tempTable-lastindex="0" perfect-tempTable-action="0"></div>
                </div>

            </div>
        </div>*@
            <hr />
            <div class="form-row">
                <div class="col-sm-12">
                    <div class="card">
                        <div class="card-body" style="padding-top:0 !important">
                            <div class="table-responsive-lg tableFixHead">

                                <table class="table table-hover table-responsive-lg table-striped  text-black" id="DepreciationDetails">
                                    <thead class="bg-primary text-white " style="position:sticky; top:0px">
                                        <tr>


                                            <th style="display:none"> </th>
                                            <th style="display:none">ItemCategory</th>
                                            <th>Category</th>
                                            <th style="display:none">ItemMaster</th>
                                            <th>Item Name</th>

                                            <th style="display:none">ItemStock</th>
                                            <th>Mode</th>

                                            <th style="display:none">DepreciationSettings</th>

                                            <th>Item Quantity</th>
                                            <th>Depreciation Period</th>

                                            <th>Depreciation Amount</th>
                                            <th>Depreciation Max Amount</th>

                                            <th></th>

                                        </tr>
                                    </thead>
                                    <tbody id="DepreciationDetailsTbody">
                                        <tr id="0">
                                            @*<td colspan="12" style="text-align:center;">No Records...</td>*@
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
            <div class="card-footer text-right" perfect-class="formFooter" id="saveclear">


                @if (ViewBag.PagedAccessRights.UsrrlMsAdd)
                {
                    <button id="addcompany-jq-addnewuser" type="button" class="btn btn-primary mr-3  d-none" name="add" perfect-css="button" onclick="fn_AddDepreciationProcess(this,'new')"><i class="fa fa-refresh fa-spin loaderClass "></i>Save</button>
                }
                <button type="button" class="btn btn-light d-none" perfect-css="button" onclick="fn_DepreciationProcessClear()" name="reset">Clear</button>
            </div>
        </form>
    </div>


</div>

<script>
    (function () {
        jQuery('select').selectpicker();
    })();
   
    var Ddata = 0;
    var DepreciationDetails = [];


    fn_DepreciationProcess

     function fn_showTypeList(ele) {

       
        let $thisForm = $(ele).closest('[perfect-class="form"]');
         let Id_Mode = $thisForm.find('[name=Mode]').val();
         $thisForm.find('[name=FK_Product]').val('')
         $thisForm.find('[name=Product]').val('');


       $.ajax({
            url: '@Url.Action("GetCategorylist", "DepreciationProcess")',
            type: "POST",
           data: JSON.stringify({ Id_Mode: Id_Mode}),//<---- Input json : data passed to controller
            dataType: "json",
           contentType: "application/json",
            success: function (ajaxOutput) {

                let $menuDropdown = $thisForm.find('[name=FK_Category]');//<--- Will give place select html element

                    $menuDropdown.empty().selectpicker('refresh');
                    $menuDropdown.append($('<option/>', { "value": "" }).text("Please Select")).selectpicker('refresh')


                    if (ajaxOutput.Process.IsProcess) {

                        $.each(ajaxOutput.Data, function (key, value) {
                            $menuDropdown.append($('<option/>', { "value": value.FK_Category }).text(value.Category)).selectpicker('refresh')
                        });
                        if ($menuDropdown.attr('perfect-onloadSelect')) {
                            $menuDropdown.val($menuDropdown.attr('perfect-onloadSelect')).selectpicker('refresh')
                        }
                    }
                    else {
                        $.each(ajaxOutput.Process.Message, function (key, value) {
                           
                        });
                    }
            }

        });



    }

    function clearproduct(ele) {
        let $thisForm = $(ele).closest('[perfect-class="form"]');
        $thisForm.find('[name=ProductID]').val(0);
        $thisForm.find('[name=Product]').val('');

    }
    function fn_DepreciationProcessClear(ele) {
        DepreciationProcessInitialLoad();
    }
    function fn_AddDepreciationProcess(ele, actionType) {
        {
            
           
            let $thisForm = $(ele).closest('[perfect-class="form"]');//<------Form element :  Our Form which contain our input elements ,saved in a variable for reusability


            //$(ele).find('i').removeClass();

            if ($thisForm.valid()) {
                let $errorMsg = '';
                //let $blnSuccess = false;

                //<---------Validation :  To Check whether the form is vaild(ie; values in the form contrls are valid)
                $(ele).prop('disabled', true);
                var tabledepreciationdetailsrws = $("#DepreciationDetailsTbody tr");
                let DepreciationProcessDetails = [];
                if (tabledepreciationdetailsrws.length > 0) {
                    $('#DepreciationDetailsTbody tr').each(function (index, ele) {
                        let DepreInfo = {};
                        DepreInfo['FK_ItemCategory'] = $(this).find('.jdFK_ItemCategory').text();
                        DepreInfo['CategoryName'] = $(this).find('.jdCategoryName').text();
                        DepreInfo['FK_ItemMaster'] = $(this).find('.jdFK_ItemMaster').text();

                        DepreInfo['ItemName'] = $(this).find('.jdItemName').text();
                        DepreInfo['FK_ItemStock'] = $(this).find('.jdFK_ItemStock').text();

                        DepreInfo['ModeName'] = $(this).find('.jdModeName').text();
                        DepreInfo['FK_DepreciationSettings'] = $(this).find('.jdFK_DepreciationSettings').text();

                        DepreInfo['ItemQty'] = $(this).find('.jdItemQty').text();
                        DepreInfo['DeprePeriod'] = $(this).find('.jdDeprePeriod').text();

                        DepreInfo['DepreAmt'] = $(this).find('.jdDepreAmt').text();
                        DepreInfo['DepreMaxAmt'] = $(this).find('.jdDepreMaxAmt').text();

                        DepreciationProcessDetails.push(DepreInfo);
                    });
                }

               

                let ID_DepreciationProcessDetails = 0;
                    let _ActionUrl = "";

                    if (actionType === "new") {
                        ID_DepreciationProcessDetails = 0;
                        _ActionUrl = "@Url.Action("AddNewDepreciationProcess", "DepreciationProcess")";
                    }
                   

                var inputData = {
                    __RequestVerificationToken: $('[name="__RequestVerificationToken"]').val(),
                    'ID_DepreciationProcessDetails': ID_DepreciationProcessDetails,
                    'DepreProcessDate': $thisForm.find('[name="ProcessDate"]').val(),
                    'DepreFromDate': $thisForm.find('[name="Fromdate"]').val(),
                    'DepreToDate': $thisForm.find('[name="Todate"]').val(),
                    'FK_Category': $thisForm.find('[name=FK_Category]').val(),
                    'Mode': $thisForm.find('[name=Mode]').val().trim(),
                    'FK_Product': $thisForm.find('[name=FK_Product]').val(),
                    'DepreciationProcessDetails': DepreciationProcessDetails

                };

                    $.ajax({//if($blnSuccess) ajax
                        url: _ActionUrl,
                        type: "POST",
                        data: inputData,
                        dataType: "json",
                        contentType: 'application/x-www-form-urlencoded; charset=utf-8',
                        success: function (ajaxOutput) {//if($blnSuccess) ajax success
                            if (ajaxOutput.Process.IsProcess) {
                                $.each(ajaxOutput.Process.Message, function (key, value) {
                                    NotificationMessage({ 'type': 'success', 'heading': 'Success', 'message': value });
                                    $(ele).prop('disabled', false);
                                    DepreciationProcessInitialLoad();
                                });
                            }
                            else {
                                $.each(ajaxOutput.Process.Message, function (key, value) {
                                    NotificationMessage({ 'type': 'error', 'heading': 'Error', 'message': value });
                                    $(ele).prop('disabled', false);
                                    DepreciationProcessInitialLoad();
                                });
                            }

                            $thisForm.find('[perfect-ctype="select"]').val('').selectpicker('refresh');
                            $thisForm.find('[perfect-ctype="input"]').val('');
                            $thisForm.find('[perfect-ctype="date"]').val('');
                            let onSuccess = $thisForm.attr('perfect-onFormSuccess')
                            if (onSuccess) {
                                window[onSuccess]();

                            }
                        },//if($blnSuccess) ajax success
                        complete: function () {
                            $(ele).prop('disabled', false);
                        }
                    });//if($blnSuccess) ajax
                //if($blnSuccess)


                //else {
                //    NotificationMessage({ 'type': 'error', 'heading': 'Error', 'message': $errorMsg });
                //    $(ele).prop('disabled', false);
                //}


            }
            else {
                    NotificationMessage({ 'type': 'error', 'heading': 'Error', 'message': $errorMsg });
                    $(ele).prop('disabled', false);
                }
        }

    }
    function checkLimit(ele) {
        let $thisForm = $(ele).closest('[perfect-class="form"]');
        let Fromdate = $thisForm.find('[name="Fromdate"]').val();
        let Todate = $thisForm.find('[name="Todate"]').val();

        Fromdate = new Date(Fromdate);
        Fromdate.setHours(0, 0, 0, 0)

        Todate = new Date(Todate);
        Todate.setHours(0, 0, 0, 0)

        if (Todate < Fromdate) {

            console.log('inside limit check if')
            $thisForm.find('[name="Fromdate"]').closest('[perfect-class="formGroup"]').find('.limit-check').text('From Date should be less than To Date');
            return false;
        }
        else {

            console.log('inside limit check else')
            $thisForm.find('[name="Fromdate"]').closest('[perfect-class="formGroup"]').find('.limit-check').text('')
            return true;
        }

    }
   
    var DepreciationDetails = [];
    function fn_DepreciationProcess(ele) {
        var $thisForm = $(ele).closest('[perfect-class="form"]');
      
        var mode = $('#Mode').val();
        var category = $('#Categoryid').val();
        if (mode == "" || category == "") {
            NotificationMessage({ 'type': 'error', 'heading': 'Error', 'message': 'Please select Mode and Category' });
        }
        else {


            
            $thisForm.find('button[name=add]').addClass('d-none');
            $thisForm.find('button[name=reset]').addClass('d-none');

            var InputData = {
                'ProcessDate': $thisForm.find('[name="ProcessDate"]').val(),
                'Fromdate': $thisForm.find('[name="Fromdate"]').val(),
                'Todate': $thisForm.find('[name="Todate"]').val(),
                'FK_Category': $thisForm.find('[name="FK_Category"]').val(),
                'FK_Product': $thisForm.find('[name="FK_Product"]').val(),
                'Mode': $thisForm.find('[name="Mode"]').val(),



            }
            $.ajax({
                url: "@Url.Action("GetDepreciationInfo", "DepreciationProcess")",
                type: "Get",
                data: InputData,
                dataType: "json",
                contentType: "application/json",
                success: function (ajaxOutput) {
                    $TicketHtml = '';
                    if (ajaxOutput != null) {
                        bindajaxout = ajaxOutput;
                        var $ProductHtml = '';

                        if (ajaxOutput.Lits.Data != null) {
                            $('#DepreciationDetailsTbody').empty();
                            if (ajaxOutput.Lits.Data.length > 0) {
                                console.log('2', ajaxOutput.Lits.Data);
                                if (ajaxOutput.Lits.Data[0].ErrCode == -2) {


                                    NotificationMessage({ 'type': 'info', 'heading': 'info', 'message': ajaxOutput.Lits.Data[0].ErrMsg});

                                }
                                else {
                                    //Ddata = ajaxOutput.Lits.Data.length;

                                    $("#saveclear").css("display", "block");
                                    $thisForm.find('button[name=add]').removeClass('d-none');
                                    $thisForm.find('button[name=reset]').removeClass('d-none');


                                    $.each(ajaxOutput.Lits.Data, function (i, value) {
                                        let rowCount = (i + 1);
                                        $ProductHtml += "<tr id=" + rowCount + ">";
                                        $ProductHtml += "<td class='jdFK_ItemCategory' Style='Display:none'>" + value.FK_ItemCategory + "</td>";
                                        $ProductHtml += "<td class='jdCategoryName' style='width: 600px';>" + value.CategoryName + "</td>";
                                        $ProductHtml += "<td class='jdFK_ItemMaster' Style='Display:none'>" + value.FK_ItemMaster + "</td>";

                                        $ProductHtml += "<td class='jdItemName' style='width: 600px;'>" + value.ItemName + "</td>";
                                        $ProductHtml += "<td class='jdFK_ItemStock' Style='Display:none'>" + value.FK_ItemStock + "</td>";

                                        $ProductHtml += "<td class='jdModeName' style='width: 600px;'>" + value.ModeName + "</td>";
                                        $ProductHtml += "<td class='jdFK_DepreciationSettings' Style='Display:none'>" + value.FK_DepreciationSettings + "</td>";

                                        $ProductHtml += "<td class='jdItemQty' style='width: 600px;'>" + value.ItemQty + "</td>";
                                        $ProductHtml += "<td class='jdDeprePeriod' style='width: 600px; text-align: center;'>" + value.DeprePeriod + "</td>";

                                        $ProductHtml += "<td class='jdDepreAmt' style='width: 600px; text-align: center;'>" + value.DepreAmt + "</td>";
                                        $ProductHtml += "<td class='jdDepreMaxAmt' style='width: 600px;  text-align: center;'>" + value.DepreMaxAmt + "</td>";
                                        $ProductHtml += "</tr>";

                                    });

                                }

                            }
                            else {
                              
                                $("#saveclear").css("display", "none");
                            }
                        }
                        else {
                            $('#DepreciationDetailsTbody').empty();
                            $ProductHtml += "<tr id='0'><td colspan='8' style'text-align:center'>No Records..</td></tr>";
                        }

                        $("#DepreciationDetails").show();
                        $('#DepreciationDetailsTbody').append($ProductHtml);


                    }
                }

            });


        }
    }

    function fn_showlists(ele) {
        debugger
        let $thisForm = $(ele).closest('[perfect-class="form"]');

        let Id_Mode = $thisForm.find('[name=Mode]').val();

        switch (Id_Mode) {
            case 'V':
                GetCmnPopUpSearchValAssign(ele, 108, 'Product List', []);
                break;
            case 'T':
                GetCmnPopUpSearchValAssign(ele, 85, 'Product List', []);
                break;

            default:
                NotificationMessage({ 'type': 'info', 'heading': 'info', 'message': 'Please Select Category' });
                break;


        }



    }

    function getfunctionAssign(ele) {
    }



  </script>